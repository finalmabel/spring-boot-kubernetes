pool:
  vmImage: ubuntu-latest
stages:

- stage: ContainerSecurityMABEL
  jobs:
  - job:
    displayName: 'Container Security'
    steps:
    - task: Maven@4
      inputs:
        mavenPomFile: 'pom.xml'
        publishJUnitResults: true
        testResultsFiles: '**/surefire-reports/TEST-*.xml'
        codeCoverageToolOption: 'JaCoCo'
        javaHomeOption: 'JDKVersion'
        mavenVersionOption: 'Default'
        mavenAuthenticateFeed: false
        effectivePomSkip: false
        sonarQubeRunAnalysis: false
    - task: DockerInstaller@0
      inputs:
        dockerVersion: '17.09.0-ce'
    - task: Docker@2
      inputs:
        containerRegistry: 'tarea_final_container'
        command: 'login'
    - task: Docker@2
      inputs:
        containerRegistry: 'tarea_final_container'
        command: 'build'
        Dockerfile: '**/Dockerfile'
        tags: 'latest'
    - script: |
        sudo apt-get install -y rpm
        wget https://github.com/aquasecurity/trivy/releases/download/v0.27.1/trivy_0.27.1_Linux-64bit.deb
        sudo dpkg -i trivy_0.27.1_Linux-64bit.deb
        trivy image -x "HIGH,CRITICAL" --exit-code 1 mabelmellado1974/tarea_final_container:latest
      displayName: Image Scan
    
    - task: Docker@2
      inputs:
        containerRegistry: 'tarea_final_container'
        repository: 'mabelmellado1974/tarea_final_container'
        command: 'push'
        tags: 'latest'